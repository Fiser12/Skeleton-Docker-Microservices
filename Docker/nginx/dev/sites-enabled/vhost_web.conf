server {
    listen   80;
    server_name app.com;
    root /app;

    location @rewriteAppApi {
        rewrite ^(.*)$ /app_dev.php/$1 last;
    }

    location @rewriteImagesApi {
        rewrite ^(.*)$ /app_dev.php/$1 last;
    }

    location /app/ {
        alias /app/App/src/App/Infrastructure/Ui/Http/Symfony;
        index app_dev.php;
        set $subfolder "App/src/App/Infrastructure/Ui/Http/Symfony/app_dev.php";
        try_files $uri @rewriteAppApi;
    }

    location /images/ {
        alias /app/Images/src/App/Infrastructure/Ui/Http/Symfony;
        index app_dev.php;
        set $subfolder "Images/src/App/Infrastructure/Ui/Http/Symfony/app_dev.php";
        try_files $uri @rewriteImagesApi;
    }

    location / {
        root /app/CompositeUi/build;
        index index.html;
    }

    location ~ ^/(app_dev|index)\.php(/|$) {
        fastcgi_pass php-upstream;
        fastcgi_split_path_info ^(.+\.php)(/.*)$;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root/$subfolder;
    }
}

